# 🚀 部署指南

## 📋 部署到 GitHub Pages 的步骤

### 第一步：创建 GitHub 仓库

1. 访问 [GitHub](https://github.com/new) 创建新仓库
2. 仓库名称：`your-username.github.io` （推荐）或任意名称
3. **不要** 勾选 "Initialize this repository with a README"
4. 点击 "Create repository"

### 第二步：推送代码到 GitHub

在项目目录执行以下命令：

```bash
# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Quartz blog with dark theme"

# 添加远程仓库（替换 YOUR_USERNAME 和 YOUR_REPO）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 推送到 GitHub（如果默认分支是 master，需要先重命名）
git branch -M main
git push -u origin main
```

### 第三步：配置 GitHub Pages

1. 进入仓库的 **Settings** → **Pages**
2. **Source** 选择：`GitHub Actions`
3. 保存设置

### 第四步：等待部署完成

1. 进入仓库的 **Actions** 标签页
2. 查看部署进度（首次大约需要 2-3 分钟）
3. 部署成功后，访问：
   - `https://your-username.github.io/your-repo/`
   - 或 `https://your-username.github.io/`（如果仓库名是 username.github.io）

---

## 🎨 当前配色方案

### 深色模式（GitHub Dark 风格）
- **背景色**: `#0d1117` (深黑色)
- **卡片背景**: `#161b22` (略浅黑色)
- **主要文字**: `#f0f6fc` (浅白色)
- **强调色**: `#58a6ff` (蓝色)
- **次要色**: `#3fb950` (绿色)

### 浅色模式
- **背景色**: `#fafbfc` (浅灰白)
- **主要文字**: `#0f172a` (深蓝黑)
- **强调色**: `#0ea5e9` (天蓝色)
- **次要色**: `#10b981` (翡翠绿)

---

## 📝 后续更新

每次修改内容后：

```bash
# 1. 添加更改
git add .

# 2. 提交
git commit -m "Update content"

# 3. 推送
git push
```

推送后，GitHub Actions 会自动重新构建和部署！

---

## 🛠️ 自定义域名（可选）

1. 在仓库添加 `public/CNAME` 文件，内容为你的域名
2. 在域名提供商处添加 DNS 记录：
   - `A` 记录指向 GitHub Pages IP
   - 或 `CNAME` 记录指向 `your-username.github.io`
3. 在 GitHub Pages 设置中填写自定义域名

---

## ✨ 已实现的功能

- ✅ 深色/浅色模式自动切换
- ✅ 深黑主题（OLED 友好）
- ✅ 响应式设计
- ✅ 全文搜索
- ✅ 知识图谱
- ✅ 反向链接
- ✅ 面包屑导航
- ✅ 目录导航
- ✅ 最近更新
- ✅ 文件浏览器
- ✅ GitHub Actions 自动部署

---

## 🎯 推荐配置

### 仓库设置
- **Description**: 添加仓库描述
- **Topics**: 添加标签（如 `blog`, `quartz`, `digital-garden`）
- **About**: 填写网站链接

### 本地开发
```bash
# 安装依赖
npm install

# 本地预览
npx quartz build --serve

# 访问 http://localhost:8080
```

---

**祝您部署顺利！** 🎉
